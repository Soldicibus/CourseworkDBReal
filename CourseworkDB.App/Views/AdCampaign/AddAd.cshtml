@model CourseworkDB.Data.Models.Ad

@{
    ViewData["Title"] = "AddAd";
}

<h1>AddAd</h1>

<h4>Ad</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddAd">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AdId" class="control-label"></label>
                <input asp-for="AdId" class="form-control" />
                <span asp-validation-for="AdId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AdCampaign.CampaignId" class="control-label"></label>
                <input asp-for="AdCampaign.CampaignId" class="form-control" />
                <span asp-validation-for="AdCampaign.CampaignId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
